{
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  },
  "scripts": {
    "build": "lerna run build",
    "install": "lerna bootstrap --hoist",
    "start": "lerna run start",
    "test": "lerna run test",
    "update": "lerna publish --skip-npm"
  }
}
